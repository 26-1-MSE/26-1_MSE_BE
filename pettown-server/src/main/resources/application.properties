spring.application.name=pettown-server

# Server
server.port=8080

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/pettown?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=es0214!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Forwarded headers (ngrok, reverse proxy 지원)
server.forward-headers-strategy=framework

# JWT
jwt.secret=pettown-secret-key-must-be-at-least-32-characters-long-enough-for-hmac-sha384-algorithm
jwt.expiration=86400000

# admin
admin.username=admin
admin.password=admin1234!

# OpenAI
openai.api.key=🔥🔥🔥
